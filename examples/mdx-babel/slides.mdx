import Slide from './spectacle-component';

# MDX + Babel + Spectacle JS Slide 👋

This example uses:

- `mdx`
- `babel` custom extensions for the React components we import
- a custom JS slide using `spectacle`
- a custom theme at `theme.js`
- a custom deck template at `template.js`

---

_Everything below this line is from a custom `<Slide />` JSX component:_

<Slide />

Notes: here are some more notes.
